# הוראות פרויקט — חברותא שירת התורה

## מערכת גרסאות (חשוב מאוד — לפעול לפי זה תמיד בפרויקט הזה)

הפרויקט הזה עובר עיצוב מחדש והוספת פיצ'רים. המשתמש פוחד להרוס דברים קיימים, ולכן כל שינוי משמעותי (עיצוב או פיצ'ר) חייב להיות מתועד כגרסה בגיט כדי שאפשר לחזור אליו בקלות.

**המנגנון מבוסס על git tags — לא על שכפול קבצים.** זה חינמי בטוקנים (הגיט שומר את זה, לא הקונטקסט), אמין, ומדויק.

קובץ יומן הגרסאות: [VERSIONS.md](VERSIONS.md) — שם רשומה קצרה על כל גרסה (מספר, תאריך, מה השתנה).

### כללי עבודה

1. **לפני שמתחילים שינוי חדש** (עיצוב/פיצ'ר) — לבדוק `git status`. אם יש שינויים לא שמורים משינוי קודם, לשמור אותם כגרסה קודם (ראה סעיף 2).

2. **אחרי שמסיימים שינוי ומאשרים שהוא עובד** (ויזואלית או פונקציונלית):
   - `git add` + `git commit` עם הודעה קצרה ותיאורית.
   - למצוא את המספר הבא: `git tag` הכי גבוה + 1.
   - ליצור טאג: `git tag -a vN -m "תיאור קצר"`.
   - להוסיף שורה בסוף [VERSIONS.md](VERSIONS.md) בפורמט הקיים (מספר, תאריך, תיאור, שם הטאג).
   - **אין צורך לשאול אישור** ליצירת commit/tag מקומיים (לא push) — זו פעולה שגרתית בפרויקט הזה. Push ל-remote כן דורש אישור כרגיל.
   - **חובה לציין למשתמש בסוף התגובה** משהו כמו "נשמר כגרסה N" — כך שהוא יידע כל הזמן איזה מספר גרסה רץ כרגע, בלי שיצטרך לשאול.

3. **כשהמשתמש מבקש "תחזיר לגרסה N"** (או ניסוח דומה — "בטל את השינוי האחרון", "חזור למה שהיה"):
   - לבדוק שהטאג `vN` קיים (`git tag`).
   - להריץ: `git checkout vN -- .` (משחזר את כל הקבצים למצב של הגרסה, בלי לגעת בהיסטוריה).
   - ליצור commit חדש: `git commit -m "revert to vN"`.
   - **לא** להשתמש ב-`git reset --hard`, `git push --force`, או מחיקת טאגים/היסטוריה — לעולם. השחזור חייב להיות הפיך.
   - לעדכן את [VERSIONS.md](VERSIONS.md) עם שורה על השחזור.
   - לדווח למשתמש בקצרה שהוחזר לגרסה N.

4. **מספור הטאגים ברצף**: v1, v2, v3... (v1 = הבסיס לפני תחילת העיצוב מחדש, 2026-09-25).
